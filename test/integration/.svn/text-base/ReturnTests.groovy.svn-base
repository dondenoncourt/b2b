import grails.test.*
import com.kettler.domain.actrcv.share.Return
import com.kettler.domain.actrcv.share.ReturnItem

class ReturnTests extends GrailsUnitTestCase {
	void testIt() {
//		def ra = new Return(compCode: '01',custNo:'AM0302W', orderNo:1)
//		if (!ra.save()) {
//			ra.errors.each { println it.inspect()}
//		}
//		def item = new ReturnItem(compCode:ra.compCode, itemNo:'chair')
//		ra.addToItems(item)
//		if (!item.validate()) {
//			item.errors.each { println it.inspect()}
//		}
//		def item2 = new ReturnItem(compCode:ra.compCode, itemNo:'desk')
//		ra.addToItems(item2)
//		if (!item2.validate()) {
//			item.errors.each { println it.inspect()}
//		}
//		assertEquals 2, Return.get(ra.id).items.size()
	}
}
